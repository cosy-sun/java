1. 下载jdk17
2. bash configure --enable-debug --with-boot-jdk=/opt/app/jdk/jdk17.0.8 --build=x86_64-unknown-linux-gnu --host=x86_64-unknown-linux-gnu
3. make compile-commands
4. make CONF=linux-x86_64.... compile-commands
5. 导入clion， 导入build下的linux
7. 更新根目录。  tool->compilation datbase->change project root ， 选择根目录