package com.cosy.sun.algorithm.cipher;

import org.junit.Test;


public class AESTest {

    @Test
    public void testAes() throws Exception {
//        AES aes = new AES();
//        String str = "this is sunzhenhua";
//        byte[] encode = aes.encode(str, AES.getSecretKeyByFactory(AES.key), AES.algorithm, AES.encoding, null);
//        System.out.println(Base64.encodeBase64String(encode));
//        byte[] decode = aes.decode(Base64.decodeBase64(Base64.encodeBase64String(encode)), AES.getSecretKeyByFactory(AES.key), AES.algorithm, AES.encoding, null);
//        System.out.println(new String(decode));
//
//        try (FileOutputStream out = new FileOutputStream(new File("E:\\encrypt.txt"));
//             CipherInputStream cis = aes.encrypt(getSecretKey(key), new FileInputStream(new File("E:\\txt.txt")), algorithm, null);) {
//            byte[] buff = new byte[1024];
//            int n = 0;
//            while (-1 != (n = cis.read(buff))) {
//                out.write(buff, 0, n);
//            }
//        } catch (Exception e) {
//            e.printStackTrace();
//        }
//
//        try (FileOutputStream out = new FileOutputStream(new File("E:\\decrypt.txt"));
//             CipherInputStream cis = decrypt(getSecretKey(key), new FileInputStream(new File("E:\\encrypt.txt")), algorithm, null);) {
//            byte[] buff = new byte[1024];
//            int n = 0;
//            while (-1 != (n = cis.read(buff))) {
//                out.write(buff, 0, n);
//            }
//        } catch (Exception e) {
//            e.printStackTrace();
//        }

    }

}