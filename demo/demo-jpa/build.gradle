group = "com.cosy.sun.jpa"
version = "${demoVersion}"

dependencies {
	compile "org.springframework.boot:spring-boot-starter-data-jpa"
	compile project(":demo-framework")
	compile project(":demo-api")
}
